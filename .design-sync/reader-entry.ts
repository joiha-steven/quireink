// The design-sync entry for the READING site's tokens-only bundle.
//
// Deliberately empty. The public site ships no React components (see gen-reader.ts), so this
// design system is a stylesheet plus fonts and nothing else; the converter still wants an
// entry to resolve the package from, and this is it.
export {}
