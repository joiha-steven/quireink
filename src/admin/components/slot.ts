// Moved to `@/admin-shared/slot` with ADR 0054's settings screen, where the server draws the
// same empty slot. Re-exported, so every call site keeps the import it had.
export { EMPTY_SLOT } from '@/admin-shared/slot'
