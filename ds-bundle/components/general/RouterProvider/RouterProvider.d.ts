import * as React from 'react';

/**
 * RouterProvider — from quireink@2.0.0.
 */
export interface RouterProviderProps {
children: ReactNode;
}

export declare const RouterProvider: React.ComponentType<RouterProviderProps>;
