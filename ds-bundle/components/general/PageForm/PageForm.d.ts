import * as React from 'react';

/**
 * PageForm — from quireink@2.0.0.
 */
export interface PageFormProps {
initial?: PageWithContent;
  contentWidth: number;
  typewriterEffects: boolean;
}

export declare const PageForm: React.ComponentType<PageFormProps>;
