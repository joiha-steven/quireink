CacheButton from quireink. Use via `window.QuireInk.CacheButton` (bundle loaded from the root `_ds_bundle.js`). Wrap the tree in `<RouterProvider>` (full provider chain in README.md — components read theme/i18n from that context).

## Props

```ts
interface CacheButtonProps {
className?: string;
  icon?: ReactNode;
  collapsed?: boolean;
}
```
